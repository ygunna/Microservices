import { CodeFilterPipe } from './code-filter.pipe';

describe('CodeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CodeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
