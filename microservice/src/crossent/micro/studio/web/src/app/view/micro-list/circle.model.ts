export class Circle {
  x: number;
  y: number;
  r: number;
  fill: string;

  constructor(x: number, y: number, r: number, fill: string) {
    this.x = x;
    this.y = y;
    this.r = r;
    this.fill = fill;
  }
}
